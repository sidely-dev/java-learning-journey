
        //============
        //   FRAME
        // ===========
        /*
        JFrame frame = new JFrame(); //Creates a Frame
        frame.setTitle("JFrame title goes here"); //SEts the title at the top
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE); //Closes the frame when we press the exit button
        frame.setResizable(false); // 
        frame.setSize(420,420); //Sets the X-dimension & Y-dimension of the frame
        frame.setVisible(true); // Makes the frame visible

        ImageIcon image = new ImageIcon("images/dog.png"); //Creates a Image Icon(Imports/Initialises a Image that use want to use)
        frame.setIconImage(image.getImage()); //Change image icon of the frame
        frame.getContentPane().setBackground(new Color(255,255,255)); //Change the backgroud color of frame

        
        */
